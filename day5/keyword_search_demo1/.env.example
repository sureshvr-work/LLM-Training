# Copy to .env and paste your key:  cp .env.example .env
# Keyword retrieval needs NO key — this is used only by the final answer step
# (and the optional LLM-driven chunking strategy).
OPENAI_API_KEY=sk-....
